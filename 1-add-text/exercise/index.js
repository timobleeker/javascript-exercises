function getLineFromInput() {
  // find the input:
  // var input = ...;

  // And return the value property of the input:
  // return ...;
}

function addLine(line) {
  // find the 'lines' ul element
  // var lines = ...;

  // Then we use Javascript to create a new li element:
  // var lineEl = ...;
  // And set the innerText property to the value of the 'line' argument:
  // lineEl.innerText = ...;

  // And add it to the ul by using "appendChild" on the 'lines' element.
  // ...
}

function onClick() {
  // Get the input value:
  // var inputValue = ...;

  // add the input value as line:
  // addLine(...);
}

window.onload = () => {
  // Find the button element:
  // var button = ...;

  // Then assign the onClick function to the button's onclick property.
}
